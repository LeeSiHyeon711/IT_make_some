# 검수 리포트 — AI-Morning-Brief

- 검수일: 2026-06-21 (V0.1 초기) → 2026-06-28 (V0.2 증분: 주간보고) → 2026-07-02 (V0.3 증분: 월간보고 #12)
- 검수자: reviewer (claude-sonnet-4-6)
- 대상 코드: `projects/AI-Morning-Brief/05-개발/`
- 프로젝트 타입: 자동화 프로세스 (Python 3.14 CLI 파이프라인)
- QA 전략: CLI 실행 검증 (격리 임시 경로 `MORNINGBRIEF_DB/RAW_DIR/REPORTS_DIR` 환경변수 override + `--force-fallback` + `--no-discord`)

---

## 최종 판정

| 범위 | 판정 |
|------|------|
| V0.1 일간 파이프라인 (기존) | **합격 (PASS)** — V0.2·V0.3 증분 후에도 회귀 없음 |
| V0.2 증분 — FEAT-09 (#14) 주간보고 집계·생성 엔진 | **합격 (PASS)** |
| V0.2 증분 — FEAT-10 (#15) Discord 다이제스트 + 스케줄러 + 통합테스트 | **합격 (PASS)** |
| V0.3 증분 — FEAT-11 (#16) 월간보고 신호·필터 엔진 | **합격 (PASS)** |
| V0.3 증분 — FEAT-12 (#17) 월간보고 합성·전문생성·오케스트레이션 | **합격 (PASS)** |
| V0.3 증분 — FEAT-13 (#18) 월간보고 Discord 다이제스트·스케줄러·통합테스트 | **합격 (PASS)** |
| **종합 판정** | **합격 (PASS) — 이슈 없음** |

---

## 1~8. V0.1/V0.2 검증 이력 (2026-06-21 → 2026-06-28, 기존 기록 유지)

> 아래는 이전 회차(주간보고 V0.2)의 검증 기록이다. 이번 회차(월간보고 V0.3)는 9절부터 이어진다.

### 1. QA 진입 조건 확인

| 항목 | 상태 |
|------|------|
| 이슈 #14 (FEAT-09) closed | 확인 (이슈등록내역.md: commit a4de683) |
| 이슈 #15 (FEAT-10) closed | 확인 (이슈등록내역.md: commit da4308f) |
| Git commit 완료 | 확인 |
| 빌드 성공(pytest 18 passed) | 확인 |
| 사람 `/승인` | 호출 시 이미 승인 완료 전제 |

### 2. 환경 확인

| 항목 | 결과 |
|------|------|
| Python 버전 | 3.14.2 (3.11+ 요구사항 충족) |
| pytest 버전 | 9.1.1 |
| 의존성 | feedparser, requests, PyYAML, anthropic, python-dateutil 모두 설치됨 |
| 격리 방식 | 모든 실행 시 `MORNINGBRIEF_DB`·`MORNINGBRIEF_RAW_DIR`·`MORNINGBRIEF_REPORTS_DIR`를 scratchpad 임시 경로로 override — 운영 DB·reports 무접촉 |

### 3. pytest 자동 테스트 (당시 시점: 18 passed)

```
python3 -m pytest -q tests/
..................                                                       [100%]
18 passed in 1.68s
```

### 4. FEAT-09 (#14) — 주간보고 집계·생성 엔진: 완료조건 6개 전부 PASS
(멱등 skip/강제 재생성, 전문 12섹션 존재, 지속줄기·버스트·노이즈·공방픽 분류 정확, 일간 회귀 없음 — 상세는 과거 판정 근거로 보존)

### 5. FEAT-10 (#15) — Discord 다이제스트 + 스케줄러 + 통합테스트: 완료조건 6개 전부 PASS
(`build_weekly_message` 9블록·619자, plist `plutil OK`/일요일 04:40, `test_weekly.py` 7 passed, README 운영 섹션 포함)

### 6. 회귀 검증 — 일간 V0.1.1 파이프라인: PASS (`test_pipeline.py` 11 passed)

### 7. 구현 범위 준수 확인: PASS (기존 함수 무수정, DB 스키마 변경 없음, 임베드/분할 전송 없음, 불필요 라이브러리 없음)

### 8. 발견 이슈 (V0.2 시점)

없음.

---

## 9. V0.3 증분 — 월간보고(#12) FEAT-11/12/13 검증 (2026-07-02)

### 9-1. QA 진입 조건 확인 (검수 시작 전)

| 항목 | 상태 | 근거 |
|------|------|------|
| 이슈 #16(FEAT-11)·#17(FEAT-12)·#18(FEAT-13) closed | 확인 | `04-깃허브/이슈등록내역.md` — 3건 모두 "닫힘", commit `350d256` |
| 모든 FEAT 완료 조건 충족 | 확인(아래 9-3~9-5) | — |
| 빌드 성공 | 확인 | `pytest -q tests/` → 44 passed |
| Git commit/push 완료 + 원격 반영 | 확인 | `git status` clean, `git log -1` = `350d256`(HEAD, origin/main과 일치), `git remote -v` = `LeeSiHyeon711/AI-Morning-Brief.git` |
| 사람 `/승인` | 완료 전제(공방장 호출) | — |

이번 회차는 GitHub 원격에도 접근을 시도했으나(`gh issue view 12 -R LeeSiHyeon711/AI-Morning-Brief`), 이 세션은 검수자로서 GitHub 쓰기 권한 훅에 의해 읽기 명령까지 차단되었다(공방 정책 — 4단계만 gh 사용). 대신 `04-깃허브/이슈등록내역.md`(#12 운영 발견 이슈 원문 요약: "월간 흐름 리포트 신뢰도/노이즈 필터 개선", 7개 요구)와 FEAT-11/12/13 문서(각 문서 상단에 `#12-1`~`#12-7` 태그로 요구사항 매핑됨)를 정본으로 대조했다. FEAT 문서 자체가 7개 요구사항을 각 절에 명시적으로 태깅하고 있어 대체 근거로 충분했다.

### 9-2. 회귀 테스트 — 전체 pytest

**명령**: `/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 -m pytest -q tests/`

**결과**:
```
............................................                             [100%]
44 passed in 2.37s
```

| 항목 | 기대 | 실제 | 판정 |
|------|------|------|------|
| 전체 통과 수 | 44 (기존 38 + 신규 6) | 44 | **PASS** |
| 회귀(기존 38건 실패) | 0건 | 0건 | **PASS** |

commit `350d256`의 `git show --stat`으로 파일 단위 확인: `src/weekly.py`, `src/pipeline.py`, 기존 `scripts/*.plist`, `tests/test_weekly.py`, `tests/test_pipeline.py`는 diff에 **전혀 등장하지 않음**(순수 추가 1452 insertions, 0 deletions) — "기존 코드 보호 원칙"이 동작 차원뿐 아니라 파일 변경 이력 차원에서도 충족됨을 확인.

### 9-3. 월간 생성(hermetic) 실행 검증

env override(`MORNINGBRIEF_DB`/`MORNINGBRIEF_RAW_DIR`/`MORNINGBRIEF_REPORTS_DIR`를 scratchpad 임시 경로로 지정)로 운영 데이터(`data/morning_brief.db`, `reports/`)와 완전히 격리한 뒤, FEAT-12/13 설계서의 시드 스크립트(28일×4소스, 범위외 1건, 정치기사 1건)를 그대로 사용해 6월치 데이터를 시딩하고 실행했다.

**주의(설계 확인)**: `--month`는 `--monthly` 없이 단독으로는 월간 분기를 타지 않는다(`main.py`가 `--weekly` 다음·일간 분기 앞에 `if args.monthly:` 로 디스패치하며, `--month`는 `--monthly`의 보조 인자). 최초 지시문의 `--month 2026-06 --no-discord --force-fallback`(단독)으로 실행했을 때 일간 파이프라인이 대신 실행되는 것을 확인했으나, 이는 **코드 버그가 아니라 CLI 설계대로의 동작**이다(FEAT-12 6-4절·완료조건에 `--monthly --month`로 명시). `--monthly --month 2026-06`으로 재실행해 정상 검증했다.

**명령**: `python3 main.py --monthly --month 2026-06 --no-discord --force-fallback`

**결과**:
```
INFO 경로 override: db/raw_dir/reports_dir = <scratchpad 경로> (env override)
INFO 월간보고 생성: 2026-06 → <scratchpad>/2026/monthly/M06.md (mode=fallback)
```
종료코드 0. `reports/2026/monthly/M06.md` 생성 확인(`ls`로 실파일 존재 확인).

| 완료 조건(FEAT-12 8장) | 판정 |
|---|---|
| `--monthly --no-discord --force-fallback` 실행 시 `reports/<YYYY>/monthly/M##.md` 생성 + 종료코드 0 | **PASS** |
| 같은 달 재실행(`--month` 없이) 시 커서 일치로 skip | **PASS** (9-6절) |
| `--month` 지정 시 커서 일치여도 강제 재생성 | **PASS** (실제로 2026-07-02 실행 시 `target_month(now)`가 자연스럽게 2026-06을 가리켜, `--month` 없는 2차 실행이 곧 멱등 테스트를 겸함) |

### 9-4. #12의 7개 개선 요구사항 — 리포트 실제 내용 대조

생성된 `M06.md`를 직접 읽어 각 요구사항을 원문 대조했다(요약이 아닌 로직 실행 결과 확인).

| # | 요구사항(#12) | 판정 | 근거(리포트 실측) |
|---|---|------|---|
| 1 | 기간 밖 기사 "범위 외" 분리 | **PASS** | 헤더 `범위외 1`, `## 0` 하단 "범위 외 참고 항목: 1건", `## 12. 범위 외 참고 항목`에 `old news — 2026-04-01` 별도 나열(집계에서 제외됨을 확인: 4월 기사가 6월 강도/태그 표에 포함되지 않음) |
| 2 | 신뢰도 라벨 `[확정]/[추정]/[주의]` | **PASS** | `## 2` 흐름 테마·`## 3` 주목 사건 각 항목에 `[추정]` 접두, `## 10` 서술에 `[확정]` 접두, 헤더에 `신뢰도 [확정 1/추정 7/주의 0]` 카운트 명시. fallback 모드는 교차출처수 기반 규칙 라벨(설계 의도대로 `[주의]`는 fallback에서 미생성 — Claude 합성 전용 라벨이므로 정상) |
| 3 | 2단계 필터 흔적(1차 관련성) | **PASS** | 헤더 `수집 114 · 1차통과 112`(정치 기사 1건이 1차 탈락), `## 0` 표에 `정치·사회 \| 1` 로 탈락 사유 명시 |
| 4 | 대표 기사 압축(상위 N) | **PASS** | 헤더 `대표 40`, `## 11. 대표 원문 링크`에 압축된 대표 목록 게시(select_top_articles_monthly가 TOP_LIMIT=40 적용) |
| 5 | 소스별 편중도 표 | **PASS** | `## 6. 소스 편중도` 표에 소스·건수·점유율%·평균중요도·노이즈여부 컬럼 존재(예: `Google \| 56 \| 50.0% \| 2.0 \| ⚠️`) |
| 6 | 편중 지배 방지(소스당 상한) | **PASS** | 직접 함수 호출로 실측: `cap_source_bias` 적용 후 `select_top_articles_monthly`의 소스별 분포가 `{'OpenAI': 17, 'Google': 17, 'VentureBeat AI': 6}`, cap=`max(3, round(112*0.15))=17` — 모든 소스가 cap 이하(17)로 확인됨. 단위테스트(`test_source_bias_cap_and_top`)로도 이중 확인 |
| 7 | 노이즈 제외 기준·건수 상단 명시 | **PASS** | 헤더 `노이즈 1`, `## 0. 분석 기반과 제외 내역` 표에 유형별 건수(`정치·사회 \| 1`) + 합계 행 |

7개 요구사항 전부 리포트 실행 결과에서 직접 확인됨.

### 9-5. Discord 다이제스트 검증 (`build_monthly_message`)

단위테스트(`test_digest_length`, mock 데이터)뿐 아니라, **9-3에서 생성한 실제 실행 데이터**(signals/basis/synthesis)를 그대로 `build_monthly_message`에 넣어 실측했다.

```
📅 **AI Morning Brief 월간 — 2026-06 (6/1~6/30)**
이번 달 대표 40건(수집 114·1차통과 112). 지속 줄기: mcp, agent

🔎 분석 기반: 수집 114 · 1차통과 112 · 대표 40 · 범위외 1 · 노이즈 1 · 신뢰도 [확정 1/추정 7/주의 0]
🌊 월간 흐름: ...
⭐ 주목 사건: ...
🌳 지속 줄기: mcp 84·28일 · agent 84·28일
⚠️ 편중/노이즈: 최다 Google 50% · 노이즈 Google 56건·평균 2.0
🎯 공방 즉시 착수: ...
🔭 다음 달 관전: mcp / agent
📄 전체 리포트: reports/2026/monthly/M06.md
```

| 항목 | 기준 | 실측 | 판정 |
|------|------|------|------|
| 길이 | ≤ 2000자 | 625자 | **PASS** |
| 블록 수(이모지 헤더) | 9블록 | 9블록(📅🔎🌊⭐🌳⚠️🎯🔭📄) 전부 출력 | **PASS** |
| plain content | 임베드/분할 없음 | 단일 문자열 | **PASS** |
| `tests/test_monthly.py::test_digest_length` | mock 데이터로도 ≤2000, "분석 기반"·"신뢰도" 포함 | PASS(44 passed에 포함) | **PASS** |

### 9-6. 멱등 트리거 검증

**시나리오**: 6월 데이터로 강제 생성(`--month 2026-06`) → 커서 `meta.last_monthly_ym`에 `"2026-06"` 기록됨 → `--month` 없이(=`target_month(now)`로 자동 산정) 재실행.

검수 실행 시점(오늘 날짜 2026-07-02) 기준 `target_month(now)`가 정확히 `(2026, 6)`을 가리켜, 별도 시각 조작 없이 자연스러운 멱등 시나리오가 재현되었다.

**2차 실행 로그**:
```
INFO 월간보고 skip — 이미 생성됨: 2026-06
```
종료코드 0, 파일 재생성되지 않음(mtime 불변) 확인. **PASS**

또한 `tests/test_monthly.py::test_cursor_idempotent`(44 passed에 포함)로 `meta` 테이블의 `last_monthly_ym` 값이 SQL 직접 조회로도 `2026-06`임을 재확인.

### 9-7. 빈 월(edge case) 안전성 확인 (설계서 7장 예외처리)

빈 DB(기사 0건)에 대해 `--monthly --month 2026-05 --no-discord --force-fallback` 실행:
```
INFO 월간보고 생성: 2026-05 → <scratchpad>/2026/monthly/M05.md (mode=fallback)
```
종료코드 0, 헤더 `수집 0 · 1차통과 0 · 대표 0 · 범위외 0 · 노이즈 0`으로 정상 렌더(크래시 없음). **PASS**

### 9-8. plist·README 확인

| 항목 | 결과 | 판정 |
|------|------|------|
| `scripts/com.itsangsang.morningbrief.monthly.plist` XML 유효성 | `plutil -lint` → OK | **PASS** |
| `StartCalendarInterval` | Day=1, Hour=4, Minute=50 (설계서 일치) | **PASS** |
| README 월간 운영 섹션 | `## 월간 흐름 리포트(--monthly) 운영` 존재, 수동 생성·plist 등록·rsync 동기화·실행 순서(일간→주간→월간) 안내 포함 | **PASS** |

### 9-9. 기존 코드 보호 원칙 준수 (동작 + 이력 이중 확인)

| 확인 방식 | 결과 |
|---|---|
| 동작 관점: `pytest -q tests/test_weekly.py tests/test_pipeline.py` | 별도 실행 없이 전체 스위트(44 passed)에 포함되어 회귀 0건 확인 |
| 이력 관점: `git show --stat 350d256` | `src/weekly.py`, `src/pipeline.py`, 기존 daily/weekly plist, `tests/test_weekly.py`, `tests/test_pipeline.py` 모두 diff에 미등장(순수 추가만 발생) |
| 실 데이터 무접촉 | 모든 실행은 env override로 scratchpad 임시 경로 사용. 운영 `data/morning_brief.db`(마지막 수정 6/28, 검수 중 불변) 및 `reports/` 하위에 `monthly/` 디렉토리 미생성 확인 |

### 9-10. 실 Claude 합성 경로에 대한 안내

이번 자동 QA는 토큰 소모·비결정성을 피하기 위해 전 구간 `--force-fallback`(mode=fallback)로 검증했다. `synthesize_monthly`가 실제 Claude를 호출하는 mode=claude 경로(신뢰도 라벨을 Claude가 직접 판단해 붙이는 経路, `[주의]` 라벨 실사용 포함)는 **자동 QA 대상에서 제외**했다. 코드 리뷰상 `_call_claude` 기존 함수를 재사용하고 예외 시 `_fallback_monthly`로 안전 흡수하는 구조(9-4의 ②에서 fallback 라벨 규칙 확인)이므로 구조적 위험은 낮으나, **실제 API 키로 한 번은 `--monthly --month <임의월>`(--force-fallback 제외)을 사람이 수동 실행해 프롬프트 응답 품질·라벨 판단 타당성·max_tokens=20000 절삭 여부를 확인할 것을 권장**한다.

### 9-11. 발견 이슈 (V0.3 월간보고)

없음.

---

## 공방장 보고 (최신 회차 — 2026-07-02, 월간보고 #12)

**종합 판정: 합격 (PASS)**

월간보고(#12) 증분 — FEAT-11(#16)·FEAT-12(#17)·FEAT-13(#18) — 자동 QA를 완료했다.

**주요 검증 결과:**
1. `pytest -q tests/` — **44 passed**(기존 38 + 신규 6), 회귀 0건. `git show --stat 350d256`로 weekly/pipeline/기존 plist/기존 테스트 파일이 diff에 없음(순수 추가)까지 이중 확인.
2. hermetic 실행(`--monthly --month 2026-06 --no-discord --force-fallback`) — `reports/2026/monthly/M06.md` 정상 생성, 종료코드 0.
3. **#12의 7개 요구사항 전부** 생성된 리포트 원문에서 직접 확인: ①범위외 분리 ②`[확정]/[추정]/[주의]` 라벨 ③2단계 필터(수집114→1차통과112) ④대표 압축(40건) ⑤소스 편중도 표 ⑥소스당 상한(cap=17, 실측 최대 17건 준수) ⑦노이즈 유형·건수 상단 명시.
4. Discord 다이제스트(`build_monthly_message`) 9블록 전부 출력, 625자(≤2000).
5. 멱등: 커서 세팅 후 `--month` 없이 재실행 → `월간보고 skip` 로그, 종료코드 0.
6. 빈 월(기사 0건) edge case 크래시 없이 정상 처리.
7. `plutil -lint` plist OK(매월 1일 04:50), README 월간 운영 섹션 포함.
8. 운영 데이터(`data/`, `reports/`)는 전 과정 env override로 격리되어 무접촉 확인.

**주의사항(FAIL 아님, 안내)**: 최초 지시문의 `--month 2026-06`(단독, `--monthly` 누락) 실행은 설계대로 일간 파이프라인으로 빠졌다 — CLI 설계 의도이며 버그 아님. `--monthly --month 2026-06`으로 정정 재실행해 검증 완료.

**사람 수동 테스트 권장 항목**: 실 Claude API 키를 사용한 mode=claude 경로(신뢰도 라벨의 실제 판단 품질, `[주의]` 라벨 실사용, max_tokens=20000 절삭 여부)는 자동 QA에서 `--force-fallback`로 대체했으므로 최소 1회 사람 수동 실행 권장.

발견 이슈: **없음**

다음 단계: 사람 수동 테스트(실사용 검증 게이트) → 통과 시 7단계 납품.
