# 검수 리포트 — AI-Morning-Brief

- 검수일: 2026-06-21 (V0.1 초기) → 2026-06-28 (V0.2 증분 추가)
- 검수자: reviewer (claude-sonnet-4-6)
- 대상 코드: `projects/AI-Morning-Brief/05-개발/`
- 프로젝트 타입: 자동화 프로세스 (Python 3.11+ CLI 파이프라인)
- QA 전략: CLI 실행 검증 (격리 임시 경로 `MORNINGBRIEF_DB/RAW_DIR/REPORTS_DIR` 환경변수 override + `--force-fallback` + `--no-discord`)

---

## 최종 판정

| 범위 | 판정 |
|------|------|
| V0.1 일간 파이프라인 (기존) | **합격 (PASS)** — V0.2 증분 후에도 회귀 없음 |
| V0.2 증분 — FEAT-09 (#14) 주간보고 집계·생성 엔진 | **합격 (PASS)** |
| V0.2 증분 — FEAT-10 (#15) Discord 다이제스트 + 스케줄러 + 통합테스트 | **합격 (PASS)** |
| **종합 판정** | **합격 (PASS) — 이슈 없음** |

---

## 1. QA 진입 조건 확인

| 항목 | 상태 |
|------|------|
| 이슈 #14 (FEAT-09) closed | 확인 (이슈등록내역.md: commit a4de683) |
| 이슈 #15 (FEAT-10) closed | 확인 (이슈등록내역.md: commit da4308f) |
| Git commit 완료 | 확인 |
| 빌드 성공(pytest 18 passed) | 확인 — 아래 3절 |
| 사람 `/승인` | 호출 시 이미 승인 완료 전제 |

---

## 2. 환경 확인

| 항목 | 결과 |
|------|------|
| Python 버전 | 3.14.2 (3.11+ 요구사항 충족) |
| pytest 버전 | 9.1.1 |
| 의존성 | feedparser, requests, PyYAML, anthropic, python-dateutil 모두 설치됨 |
| 격리 방식 | 모든 실행 시 `MORNINGBRIEF_DB`·`MORNINGBRIEF_RAW_DIR`·`MORNINGBRIEF_REPORTS_DIR`를 scratchpad 임시 경로로 override — 운영 DB·reports 무접촉 |

---

## 3. pytest 자동 테스트 전체 실행

### 명령

```
python3 -m pytest -q tests/
```

### 결과

```
..................                                                       [100%]
18 passed in 1.68s
```

| 테스트 파일 | 기대 | 실제 | 판정 |
|-------------|------|------|------|
| `tests/test_pipeline.py` (일간 V0.1) | 11 passed | 11 passed | PASS |
| `tests/test_weekly.py` (주간 V0.2) | 7 passed | 7 passed | PASS |
| **합계** | 18 passed | 18 passed | **PASS** |

---

## 4. FEAT-09 (#14) — 주간보고 집계·생성 엔진 검증

### 4-1. 완료 조건 체크리스트

| # | 완료 조건 | 판정 |
|---|-----------|------|
| 1 | `python main.py --weekly --no-discord --force-fallback` 실행 시 `reports/<ISO연도>/weekly/W##.md` 생성·종료코드 0 | PASS |
| 2 | 같은 주 `--weekly`(--week 없이) 재실행 시 `meta.last_weekly_iso_week` 일치로 skip | PASS |
| 3 | `--week 2026-W26` 지정 시 커서 일치여도 강제 재생성 | PASS |
| 4 | 전문에 강도 추이·태그 지속성·소스 신호품질·공방 픽·일간 아이디어 통합 표/섹션 모두 존재 | PASS |
| 5 | `aggregate_week`가 지속 줄기(day_span≥5)·단발 버스트·노이즈 소스·공방 픽 올바르게 분류 | PASS |
| 6 | 기존 일간 QA(`pytest -q tests/test_pipeline.py`) 전부 통과 (회귀 없음) | PASS |

### 4-2. 주간 생성 실행 결과

**명령:**
```bash
MORNINGBRIEF_DB=<scratchpad>/wk_qa.db \
MORNINGBRIEF_REPORTS_DIR=<scratchpad>/rpt_qa \
MORNINGBRIEF_RAW_DIR=<scratchpad>/raw_qa \
  python3 main.py --weekly --week 2026-W26 --no-discord --force-fallback
```

**출력:**
```
2026-06-28 16:51:05 INFO 경로 override: db = .../wk_qa.db (env MORNINGBRIEF_DB)
2026-06-28 16:51:05 INFO 경로 override: raw_dir = .../raw_qa (env MORNINGBRIEF_RAW_DIR)
2026-06-28 16:51:05 INFO 경로 override: reports_dir = .../rpt_qa (env MORNINGBRIEF_REPORTS_DIR)
2026-06-28 16:51:05 INFO 주간보고 생성: 2026-W26 → .../rpt_qa/2026/weekly/W26.md (mode=fallback)
```

종료코드: 0

**파일 경로 확인:** `reports/2026/weekly/W26.md` 생성 확인

**전문 섹션 헤더 목록:**
```
## 이번 주 한 줄 요약
## 일자별 수집 강도 추이
## 태그 빈도 × 지속성
## 소스 신호 품질
## 공방 관련 픽 (relevance ≥ 7)
## 주요 흐름 테마
## 주목 사건
## 공방 즉시 착수
## 다음 주 관전 포인트
## 일간 아이디어 통합 (7일)
## 주간 흐름 서술
## 상위 원문 링크
```

모든 필수 섹션 존재 확인.

**fallback 배지 확인:**
```
> **AI 합성 실패 / 기본 주간 리포트**
```

**태그 지속성 표 — mcp/agent가 7일 지속줄기로 분류:**
```
| mcp   | 21 | 7 | 지속 줄기 |
| agent | 21 | 7 | 지속 줄기 |
```

**공방 픽 (relevance ≥ 7) — 7건 모두 포함:**
```
- [MCP agent 6-0](u6-0) — 중요도 9, 관련도 8
... (7건)
```

### 4-3. 멱등(skip) 검증

**2차 실행 (--week 없이, 커서=2026-W26):**
```
2026-06-28 16:51:39 INFO 주간보고 skip — 이미 생성됨: 2026-W26
```
종료코드 0, 커서 `2026-W26` 유지 확인.

**3차 실행 (--week 2026-W26 강제):**
```
2026-06-28 16:51:45 INFO 주간보고 생성: 2026-W26 → .../W26.md (mode=fallback)
```
커서 일치 시에도 `--week` 지정으로 재생성 확인.

### 4-4. 구현 파일 존재 확인

| 파일 | 상태 |
|------|------|
| `src/weekly.py` (신규) | 존재 — `run_weekly`, `aggregate_week`, `collect_daily_ideas`, `select_top_articles` 등 전부 구현 |
| `src/analyzer.py` 추가 함수 | `synthesize_weekly`, `_fallback_weekly`, `_build_weekly_prompt`, `_parse_weekly_response` 하단에 추가 확인. 기존 일간 함수 무수정 |
| `src/reporter.py` 추가 함수 | `save_bucket_report`, `build_weekly_report` 추가 확인. 기존 `save_report`·`build_report` 무수정 |
| `main.py` 수정 | `--weekly`·`--week` 인자 추가, `run_weekly` 분기(일간보다 먼저) 확인 |

---

## 5. FEAT-10 (#15) — Discord 다이제스트 + 스케줄러 + 통합테스트 검증

### 5-1. 완료 조건 체크리스트

| # | 완료 조건 | 판정 |
|---|-----------|------|
| 1 | `--weekly --no-discord --force-fallback` 후 보고서 생성 (FEAT-09 회귀 확인) | PASS |
| 2 | `build_weekly_message` 출력이 9블록 순서이고 길이 ≤ 2000 | PASS |
| 3 | `scripts/com.itsangsang.morningbrief.weekly.plist` 유효 XML + 일요일(Weekday 0) 04:40 설정 | PASS |
| 4 | `pytest -q tests/test_weekly.py` 전부 통과 | PASS |
| 5 | 기존 `pytest -q tests/test_pipeline.py` 회귀 없음 | PASS |
| 6 | README에 주간 plist 등록 + `~/AI-Morning-Brief-run` 동기화 절차 포함 | PASS |

### 5-2. `build_weekly_message` 9블록 포맷 검증

**테스트 입력:** 9블록 전체 mock 데이터(노이즈 소스 포함, 지속 줄기 6개, 흐름 테마 4개 등)

**출력:**
```
📅 **AI Morning Brief 주간 — 2026-W26 (6/22~6/28)**
에이전트·MCP가 지속 줄기로 굳어지고, 모델 릴리스 노이즈는 잦아듦.

📊 강도: 총 142건 · 피크 06-24 31건 · ⚠️ 단절일 1일
🌳 지속 줄기: mcp 28회·6일 · agent 24회·6일 · claude 19회·5일 · openai 15회·5일 · eval 11회·5일 · rag 9회·5일
⚠️ 노이즈 소스: VentureBeat AI 38건·평균 2.4
🧵 주요 흐름:
• MCP 서버 표준화가 IDE·CI로 확산
• 에이전트 신뢰성 평가 프레임워크 경쟁
• 코드 리뷰 자동화 실전 도입 사례 증가
• 온디바이스 소형모델 추론 비용 급락
⭐ 주목 사건:
1. Anthropic MCP 레지스트리 공개
2. OpenAI 에이전트 SDK 안정화 릴리스
3. GitHub Actions용 AI 리뷰 봇 GA
🎯 공방 즉시 착수:
1. MCP 레지스트리로 사내 도구 연결 PoC
2. 주간보고 파이프라인에 eval 지표 추가
3. PR 자동 리뷰봇 시범 도입
🔭 다음 주 관전: MCP 레지스트리 생태계 반응 / 에이전트 eval 표준화
📄 전체 리포트: reports/2026/weekly/W26.md
```

| 항목 | 기준 | 실제 | 판정 |
|------|------|------|------|
| 총 길이 | ≤ 2000자 | 619자 | PASS |
| 블록 수 (이모지 헤더) | 9블록 | 9블록 (📅📊🌳⚠️🧵⭐🎯🔭📄) | PASS |
| 블록 순서 | 스펙 E 순서 일치 | 일치 | PASS |
| 조건부 생략 | noise_sources 빈 경우 ⚠️ 블록 생략 | 정상 생략(8블록) | PASS |

비고: noise_sources가 비어 있으면 ⚠️ 노이즈 소스 블록이 생략되어 8블록이 된다. 이는 설계 의도("블록 항목이 비면 해당 블록을 생략")에 부합하며 이슈가 아니다. 전체 데이터 시 9블록 정상 출력.

### 5-3. plist XML 유효성 검증

```
plutil -lint scripts/com.itsangsang.morningbrief.weekly.plist
→ OK
```

| 항목 | 기준 | 결과 | 판정 |
|------|------|------|------|
| XML 유효성 | plutil OK | OK | PASS |
| Weekday | 0 (일요일) | 0 | PASS |
| Hour | 4 | 4 | PASS |
| Minute | 40 | 40 | PASS |
| RunAtLoad | false | false | PASS |
| KeepAlive | false | false | PASS |

### 5-4. 통합 테스트 (`tests/test_weekly.py`) 7개 항목

**명령:** `python3 -m pytest -q tests/test_weekly.py -v`

```
tests/test_weekly.py::test_iso_week_on_time_and_late PASSED
tests/test_weekly.py::test_parse_week_arg PASSED
tests/test_weekly.py::test_iso_week_bounds PASSED
tests/test_weekly.py::test_aggregate_signals PASSED
tests/test_weekly.py::test_empty_week_safe PASSED
tests/test_weekly.py::test_report_created_and_path PASSED
tests/test_weekly.py::test_cursor_idempotent PASSED
7 passed in 0.39s
```

| 테스트 | 검증 내용 | 판정 |
|--------|-----------|------|
| `test_iso_week_on_time_and_late` | 일요일 정시·+1/+2일 지연 모두 W26 산정 | PASS |
| `test_parse_week_arg` | `2026-W26` 파싱 + 형식 오류 ValueError | PASS |
| `test_iso_week_bounds` | 월요일(isoweekday=1)~일요일(7), 6일 간격 | PASS |
| `test_aggregate_signals` | 21건 총계, mcp/agent 7일 지속줄기, VentureBeat AI 노이즈, workshop_picks 7건 | PASS |
| `test_empty_week_safe` | 빈 주(0건) 안전 반환, sparse_days 7일 모두 | PASS |
| `test_report_created_and_path` | W26.md 생성·경로 일치 | PASS |
| `test_cursor_idempotent` | 1차 생성 후 커서=2026-W26, 재실행 skip | PASS |

### 5-5. Discord hook 연결 확인 (코드 정적 확인)

`src/weekly.py`의 `run_weekly` 내 FEAT-10 hook 구현 확인:
```python
# ── FEAT-10: Discord 주간 다이제스트 전송 ──
if not getattr(args, "no_discord", False):
    from src.notifier import build_weekly_message
    msg = build_weekly_message(week_key, monday, sunday, signals,
                               result["synthesis"], path)
    send_discord(secrets.get("DISCORD_WEBHOOK_URL"), msg)
```

`--no-discord` 플래그 시 전송 생략, 웹훅 URL 미설정 시 `send_discord`가 WARNING 로그 후 False 반환(파이프라인 지속). 정책 일치.

### 5-6. README 주간 운영 섹션 확인

| 항목 | 확인 내용 | 판정 |
|------|-----------|------|
| 주간 운영 섹션 존재 | `## 주간 흐름 리포트 (--weekly) 운영` 섹션 확인 | PASS |
| plist 등록 절차 | `launchctl bootstrap gui/$(id -u) ...weekly.plist` 포함 | PASS |
| `~/AI-Morning-Brief-run` 동기화 | `rsync -a` 절차 포함 | PASS |
| 수동 생성 안내 | `python main.py --weekly`, `--week 2026-W26` 예시 | PASS |

---

## 6. 회귀 검증 — 일간 V0.1.1 파이프라인

### 6-1. 일간 실행 (격리 환경)

**명령:**
```bash
MORNINGBRIEF_DB=<scratchpad>/daily_qa.db \
MORNINGBRIEF_REPORTS_DIR=<scratchpad>/daily_rpt \
MORNINGBRIEF_RAW_DIR=<scratchpad>/daily_raw \
  python3 main.py --test --no-discord --force-fallback
```

**결과:**
```
2026-06-28 16:52:48 INFO fallback 분석 사용 (force=True, key=True)
2026-06-28 16:52:48 INFO 완료: 신규 5건, 모드 fallback, 리포트 .../2026/06/28.md
```

종료코드 0, 일간 리포트 `reports/2026/06/28.md` 생성 확인.

### 6-2. 일간 pytest 회귀

```
python3 -m pytest -q tests/test_pipeline.py
11 passed in 1.33s
```

V0.2 증분 후에도 기존 11개 테스트 전부 통과. 일간 코드 수정 없음.

---

## 7. 구현 범위 준수 확인

| 항목 | 기준 | 판정 |
|------|------|------|
| 기존 일간 코드 (`analyze`, `_call_claude`, `build_report`, `save_report`, `pipeline.run`) 수정 없음 | 추가만 해야 함 | PASS — 모든 기존 함수 무수정 확인 |
| DB 스키마 변경 없음 | `meta` 테이블 기존 key-value만 사용 | PASS — `last_weekly_iso_week` key만 추가 |
| Discord 임베드/분할 전송 없음 | plain content 단일 POST | PASS |
| 불필요한 라이브러리 추가 없음 | 표준 라이브러리 + 기존 의존성만 | PASS |
| `--weekly` 분기가 일간보다 먼저 디스패치 | main.py 구조 | PASS |

---

## 8. 발견 이슈

없음.

---

## 공방장 보고

**종합 판정: 합격 (PASS)**

V0.2 증분(FEAT-09 #14 + FEAT-10 #15) 검증을 완료하였습니다.

**주요 검증 결과:**

1. `pytest -q tests/` — 18 passed (기존 11 + 신규 7). 모두 통과.
2. 격리 환경에서 `--weekly --week 2026-W26 --no-discord --force-fallback` 실행 시 `reports/2026/weekly/W26.md` 정상 생성, 종료코드 0.
3. 전문에 12개 섹션 전부 존재(강도 추이·태그 지속성·소스 신호품질·공방 픽·아이디어 통합 포함), fallback 배지 정상.
4. 멱등: 2차 실행(--week 없이) → `주간보고 skip` 로그 + 커서 유지. `--week` 지정 시 강제 재생성.
5. `build_weekly_message` 9블록 순서 충족, 619자 (≤ 2000).
6. plist XML `plutil OK` + Weekday=0 / Hour=4 / Minute=40 확인.
7. README 주간 운영 섹션(plist 등록 + rsync 동기화 절차) 포함.
8. 기존 일간 파이프라인 회귀 없음.

발견 이슈: **없음**

다음 단계: 사람 수동 테스트(실사용 검증 게이트).
