<!--
3단계 설계자 산출물. AI-Morning-Brief V0.1.
형태(자동화 프로세스)와 스택을 여기서 확정한다. 작업 분해(10장)는 FEAT 문서와 1:1.
-->

# 설계서 — AI-Morning-Brief (V0.1)

- 작성일: 2026-06-21
- 근거 문서: `02-기획/PRD.md`
- 형태: **자동화 프로세스 (Python CLI 파이프라인 + 로컬 스케줄러)**
- 목표: "검증 가능한 자동화 파이프라인" — 수집 → SQLite 저장 → Claude 분석/요약 → Markdown 리포트 → Discord 전송, 단계별 독립 검증 가능.

---

## 0. 핵심 설계 결정 요약 (TL;DR)

| 결정 항목 | 선택 | 한 줄 근거 |
|-----------|------|-----------|
| 결과물 형태 | 자동화 프로세스 (헤드리스 CLI) | UI 없이 Discord 수신 + 로컬 파일 확인이 요구사항. 가장 빠르게 검증 가능 |
| 언어 | Python 3.11+ | PRD 명시. RSS/SQLite/HTTP 생태계 성숙 |
| 설정 파일 형식 | YAML | 소스 목록 가독성·주석 가능, 운영자가 직접 추가/제거 쉬움 |
| 설정 분리 | `config.yaml`(일반설정) + `sources.yaml`(소스목록) + `sources.example.yaml`(예시) | 소스 관리와 일반 설정 분리. 실제 URL은 운영자가 sources.yaml에서 수정 |
| DB | SQLite 단일 파일 (`sqlite3` 표준 라이브러리) | PRD 명시. 개인용 단일 사용자, 누적 저장에 충분 |
| RSS 파싱 | `feedparser` | RSS/Atom 양쪽을 관용적으로 파싱, 사실상 표준 |
| HTTP/Discord | `requests` | Discord Webhook POST + 진단용 HTTP 점검에 단순 |
| **Claude 연동** | **Claude API (`anthropic` Python SDK)** | 무인 cron 실행 안정성, 토큰/비용 명시적 제어, JSON 구조화 출력 파싱 안정. (Claude Code headless는 V0.2 확장) |
| **모델명 정책** | config 기본값 + **잘못된 모델명이면 fallback으로 강등(중단 없음)** | 모델명 오타/미존재로 파이프라인이 죽지 않게. README에 "사용 가능한 Anthropic 모델명으로 수정" 안내 |
| **스케줄러** | **launchd** (macOS 네이티브) | Mac 슬립 중 놓친 작업을 깨어날 때 실행 → catch-up 정책과 자연스럽게 맞물림. (cron은 슬립 시 그냥 건너뜀) |
| operator_profile | **Claude 분석 프롬프트의 필수 입력** | 기사 중요도·상상공방 관련도·액션 판단의 "렌즈". 없으면 경고 + 내장 기본 프로필 fallback |
| 분석 실패 정책 | raw JSON·DB 저장은 항상 선행 완료 → fallback 요약으로 파이프라인 지속 | 데이터 유실 0, 다음 실행에서 재분석 |
| **`--dry-run` 무상태** | DB·raw JSON·report·Discord **어떤 것도 변경하지 않음** | 계획/미리보기 전용. 운영 데이터 무해 보장 |
| **소스 접근성 진단** | **`python main.py --check-sources`** (파이프라인 본체와 분리) | 웹 접근 제약 환경에서 실제 RSS URL 검증을 별도 명령으로 분리. 본 파이프라인은 `--test` fixtures로 외부 의존 없이 QA 통과 |

---

## 1. 최종 V0.1 아키텍처

```
                         ┌─────────────────────────────────────────┐
                         │  main.py (CLI 진입점 / 옵션 파싱)         │
                         │  --test --dry-run --no-discord            │
                         │  --force-fallback --from --to             │
                         │  --check-sources  (진단 분기, 본체와 분리) │
                         └───────────────┬───────────────────────────┘
                                         │ args
                          ┌──────────────┴───────────────┐
              --check-sources?                         (정상 실행)
                    │ yes                                 │ no
                    ▼                                     ▼
        collector.check_sources()          ┌─────────────────────────────────┐
        → 진단표 출력 → 종료(파이프라인 X)  │  pipeline.run(args) (오케스트레이션)│
                                           └─┬───┬───┬───┬───┬───┬────────────┘
       config_loader                         │   │   │   │   │   │
       ┌──────────────────────────────────────┘   │   │   │   │
       │ config.yaml / sources.yaml                 │   │   │   │
       │ operator_profile.md ◄──────────────────────┼───┤   │   │ (analyzer 핵심 입력)
       │ .env (secrets)                              │   │   │   │
       ▼                                             ▼   ▼   ▼   ▼
  [1 수집]      [2 저장]    [3 분석]   [4 리포트]   [5 전송]
  collector ─► storage ─► analyzer ─► reporter ─► notifier
   │  │          │ (SQLite) │            │           │
   │  └─raw JSON │          │            │           │
   ▼            ▼          ▼            ▼           ▼
 data/raw/  morning_brief.db  (Claude    reports/   Discord
                              or fallback) <date>/    Webhook

흐름 불변식:
  - 수집/raw JSON/DB 저장은 분석(Claude) 성공 여부와 무관하게 완료된다. (단, --dry-run은 예외: 아무것도 안 씀)
  - 단일 소스 수집 실패는 건너뛰고 성공 소스만으로 리포트를 계속 생성한다.
  - Claude 실패(모델명 오류 포함) → fallback 요약으로 리포트·전송 계속.
  - Discord 실패 → 로그만, 파이프라인은 DB/파일 기준으로 성공 판정.
  - 성공 시에만 meta.last_success_at 갱신 (catch-up 기준).
```

각 모듈은 **순수 함수 중심**으로 만들어 단독 테스트가 가능하고, 상태 변경(DB 쓰기/파일 저장/전송)은 `pipeline.py`가 조율한다. `--dry-run`은 **수집 직후 계획·미리보기만 출력하고 어떤 상태도 변경하지 않는다(무상태)** — raw JSON 저장조차 하지 않는다. `--check-sources`는 파이프라인을 타지 않고 외부 RSS 접근성만 점검하고 종료한다.

---

## 2. 폴더 구조

실제 코드는 공방 헌법에 따라 `projects/AI-Morning-Brief/05-개발/` 아래에 만든다. 아래는 그 내부 구조다.

```
05-개발/
├── README.md                  # 설치·실행·옵션 안내 (+ 최초 실행 전 --check-sources 절차, 모델명 수정 안내)
├── requirements.txt           # feedparser, requests, PyYAML, anthropic, python-dateutil
├── .gitignore                 # data/, reports/, .env, config/sources.yaml, __pycache__ 제외
├── main.py                    # CLI 진입점 (argparse → check-sources 분기 / pipeline.run)
├── config/
│   ├── config.yaml            # 일반 설정 (경로/수집/분석/Discord)  ※소스 목록 없음
│   ├── sources.yaml           # 실제 소스 목록 (운영자가 수정, gitignore)
│   ├── sources.example.yaml   # 소스 구조 예시 (커밋됨, 복사해서 sources.yaml 생성)
│   ├── operator_profile.md    # 운영자·상상공방 프로필 (Claude 분석 핵심 입력)
│   └── .env.example           # ANTHROPIC_API_KEY, DISCORD_WEBHOOK_URL 템플릿
├── src/
│   ├── __init__.py
│   ├── config_loader.py       # config/sources/profile/secrets 로드 + DEFAULT_PROFILE + 경로 env override
│   ├── storage.py             # SQLite 스키마/저장/중복제거/meta
│   ├── collector.py           # RSS 수집 + raw JSON 저장(save_raw 옵션) + check_sources 진단 (+ --test fixtures)
│   ├── analyzer.py            # Claude 분석 + fallback + 프롬프트 빌드
│   ├── reporter.py            # Markdown 리포트 생성/저장
│   ├── notifier.py            # Discord Webhook 전송 + 2000자 브리핑
│   └── pipeline.py            # 단계 오케스트레이션 + catch-up + dry-run(무상태) + check-sources 디스패치
├── data/                      # (gitignore) 런타임 생성
│   ├── morning_brief.db        #  운영 DB
│   └── raw/<YYYY-MM-DD>/<source>.json
├── reports/                   # (gitignore) <YYYY-MM-DD>/report.md
├── tests/
│   ├── fixtures/
│   │   └── sample_articles.json
│   └── test_pipeline.py       # QA-1~QA-6 자동 검증 (★ 운영 데이터와 분리된 경로 사용)
├── scripts/
│   └── com.itsangsang.morningbrief.plist  # launchd 설정 예시
└── 빌드노트.md                # builder가 채우는 빌드 기록
```

> `data/`, `reports/`, `.env`, `config/sources.yaml`는 `.gitignore`로 제외(누적 데이터·민감정보·환경별 소스). `config/config.yaml`, `config/sources.example.yaml`, `config/operator_profile.md`, `config/.env.example`는 커밋한다.
> `sources.yaml`이 없으면 로더가 `sources.example.yaml`을 fallback으로 읽고 WARNING을 남긴다(최초 실행 보호).
> **경로 env override**: 환경변수 `MORNINGBRIEF_DB` / `MORNINGBRIEF_RAW_DIR` / `MORNINGBRIEF_REPORTS_DIR` 가 설정되면 `config.yaml`의 paths.db/raw_dir/reports_dir를 덮어쓴다. 테스트가 운영 데이터를 건드리지 않도록 하는 격리 수단(섹션 9-2).

---

## 3. DB 스키마

SQLite 단일 파일 `data/morning_brief.db`. 테이블 2개 (+ 인덱스).

```sql
-- 기사 테이블: URL이 PK라 중복 수집은 INSERT OR IGNORE로 자동 차단
CREATE TABLE IF NOT EXISTS articles (
    url           TEXT PRIMARY KEY,      -- 중복 방지 키
    title         TEXT NOT NULL,
    published_at  TEXT,                  -- ISO8601, 파싱 실패 시 NULL 허용
    source        TEXT NOT NULL,         -- sources.yaml의 소스 name
    collected_at  TEXT NOT NULL,         -- ISO8601, 수집 시각
    raw_excerpt   TEXT,                  -- 원문 요약/본문 일부 (fallback 요약 재료)
    summary       TEXT,                  -- Claude 또는 fallback 요약 (분석 후 채움)
    tags          TEXT,                  -- JSON 배열 문자열 예: ["claude","mcp"]
    importance    INTEGER DEFAULT 0,     -- 0~5 중요도 (미분석 0)
    relevance     INTEGER DEFAULT 0,     -- 0~5 상상공방 관련도 (미분석 0)
    analyzed      INTEGER DEFAULT 0      -- 1=Claude 분석 성공, 0=미분석/fallback
);
CREATE INDEX IF NOT EXISTS idx_articles_published ON articles(published_at);
CREATE INDEX IF NOT EXISTS idx_articles_collected ON articles(collected_at);

-- 메타 key-value: catch-up 기준 시각 등 파이프라인 상태
CREATE TABLE IF NOT EXISTS meta (
    key   TEXT PRIMARY KEY,
    value TEXT
);
-- 사용 키:
--   last_success_at         : 마지막 성공 실행 완료 시각 (catch-up 기준, ISO8601)
--   last_report_range_start : 마지막 리포트 수집 범위 시작
--   last_report_range_end   : 마지막 리포트 수집 범위 끝
```

**중복 제거**: `INSERT OR IGNORE INTO articles(url,...) VALUES(...)` 로 URL 충돌 시 무시. `insert_article`은 `cursor.rowcount`로 신규 삽입 여부(True/False)를 돌려준다.

**기사 dict 표준 형태** (모듈 간 계약):
```python
{
  "url": str, "title": str, "published_at": str|None,
  "source": str, "collected_at": str, "raw_excerpt": str
}
```
분석 결과는 별도 dict로 합쳐 DB에 update: `{"url","summary","tags"(list),"importance","relevance","analyzed"}`.

---

## 4. 설정 파일 구조

소스 목록(`sources.yaml`)과 일반 설정(`config.yaml`)을 분리한다. 소스는 운영자가 자주 바꾸고 환경별로 다를 수 있어 별도 파일 + 예시 파일로 둔다.

### 4-1. `config/config.yaml` (커밋됨, 민감정보·소스목록 없음)

```yaml
paths:
  db: "data/morning_brief.db"               # env MORNINGBRIEF_DB로 override 가능
  raw_dir: "data/raw"                       # env MORNINGBRIEF_RAW_DIR로 override 가능
  reports_dir: "reports"                    # env MORNINGBRIEF_REPORTS_DIR로 override 가능
  operator_profile: "config/operator_profile.md"
  sources: "config/sources.yaml"            # 없으면 sources.example.yaml fallback

collection:
  default_lookback_hours: 24    # last_success_at이 없을 때 기본 수집 범위
  request_timeout_sec: 15       # 소스당 타임아웃 (초과 시 해당 소스 skip)

analysis:
  # Anthropic 모델명. 잘못된/미존재 모델명이면 호출 실패 → fallback로 강등(파이프라인 중단 없음).
  # 사용 가능한 모델명은 README 참고하여 수정. (예: claude-sonnet-4-5, claude-opus-4-1 등)
  model: "claude-sonnet-4-5"
  max_articles: 40              # 한 번에 Claude에 보낼 최대 기사 수 (토큰 제어)
  max_output_tokens: 4000

discord:
  char_limit: 2000              # Discord 메시지 한도
  truncate_marker: "…(이하 생략, 전체는 로컬 리포트 참조)"
```

### 4-2. `config/sources.example.yaml` (커밋됨) / `config/sources.yaml` (gitignore, 운영자 수정)

`sources.example.yaml`을 복사해 `sources.yaml`을 만들고 실제 URL을 검증/수정한다.
`enabled: false`인 소스는 수집·진단에서 건너뛴다(끄고 켜기 쉽게).

```yaml
# 수집 소스 목록 — 추가/제거/온오프는 이 파일만 수정
# ※ 설계 환경에서 웹 접근이 막혀 URL을 실검증하지 못했다. 최초 실행 전
#   `python main.py --check-sources`로 접근성을 확인하고 죽은 피드는 교체할 것.
sources:
  - name: "Hugging Face Blog"
    url: "https://huggingface.co/blog/feed.xml"
    enabled: true
  - name: "GitHub Blog"
    url: "https://github.blog/feed/"
    enabled: true
  - name: "Simon Willison"
    url: "https://simonwillison.net/atom/everything/"
    enabled: true
  - name: "Google Research"
    url: "https://research.google/blog/rss/"
    enabled: true
  - name: "TechCrunch AI"
    url: "https://techcrunch.com/category/artificial-intelligence/feed/"
    enabled: true
  - name: "VentureBeat AI"
    url: "https://venturebeat.com/category/ai/feed/"
    enabled: true
  - name: "The Verge AI"
    url: "https://www.theverge.com/rss/ai-artificial-intelligence/index.xml"
    enabled: true
```

> 위 URL은 **예시/후보**다. 부분 실패 정책(섹션 8)으로 죽은 피드는 자동 건너뜀이 보장되며, `--check-sources`(섹션 9-1)로 실제 접근성을 점검해 교체한다.

### 4-3. `config/.env.example` (커밋됨, 값은 비움)

```
ANTHROPIC_API_KEY=sk-ant-...        # Claude API 키 (없으면 분석 fallback)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...   # 없으면 전송 skip + 경고
```

실제 `.env`는 `.gitignore`. 코드에 하드코딩 금지.

### 4-4. `config/operator_profile.md` (커밋됨) — 섹션 6 참조

---

## 5. Claude 분석 프롬프트 구조

Claude API 단일 호출로 (1) 기사별 메타 분석 + (2) 전체 브리핑을 **하나의 JSON**으로 받는다. 파싱 안정성을 위해 JSON 출력을 강제한다.

### 5-1. 프롬프트 조립 (`analyzer._build_prompt`)

```
[system]
당신은 IT상상공방 운영자의 'AI 동향 분석가'다. 아래 <운영자 프로필>을 분석의 렌즈로
삼아 각 기사의 중요도와 상상공방 적용 가능성을 평가하라.
반드시 지정된 JSON 스키마만 출력하라. 그 외 텍스트 금지.

<운영자 프로필>
{operator_profile.md 전체 내용}   ← 핵심 입력 (섹션 6)
</운영자 프로필>

[user]
다음은 오늘 수집된 기사 목록이다. 각 기사를 평가하고 전체 브리핑을 작성하라.

<기사 목록 (JSON)>
[
  {"idx":0,"title":"...","source":"...","published_at":"...","url":"...","excerpt":"..."},
  ...
]
</기사 목록>

요구 출력 스키마:
{
  "articles": [
    {"url": "<원본 url 그대로>", "summary": "<한국어 2~3문장>",
     "tags": ["키워드", ...], "importance": <0-5 정수>, "relevance": <0-5 정수>}
  ],
  "briefing": {
    "headline_changes": ["오늘의 핵심 변화 3~5개"],
    "sangsang_ideas": ["상상공방 적용 아이디어"],
    "action_items": ["실험해볼 액션 후보"],
    "summary_text": "<Discord용 핵심 브리핑, 1800자 이내 한국어>"
  }
}
```

- `importance` = 업계 영향도, `relevance` = 상상공방(운영자 프로필 기준) 적용 가능성.
- `summary_text`는 Discord 본문 재료(notifier가 메타 정보를 덧붙여 2000자 내로 마감).
- 모델/토큰 한도는 `config.yaml: analysis.*`에서 조정. 모델명이 잘못되면 호출이 실패하지만 fallback로 내려가 파이프라인은 계속된다(섹션 8).

### 5-2. 응답 파싱

- `anthropic` SDK 응답에서 텍스트 추출 → `json.loads`.
- 코드펜스(```json … ```)가 섞여 와도 견디도록 첫 `{` ~ 마지막 `}` 슬라이스 후 파싱(방어적).
- 파싱 실패/누락 필드 → **fallback로 강등**(섹션 8). 부분 누락 필드는 기본값(importance/relevance=0, summary=raw_excerpt 일부)으로 보정.

---

## 6. operator_profile.md 사용 방식 ★

`config/operator_profile.md`는 **선택 참고문서가 아니라 Claude 분석의 핵심 입력 문서**다.

1. **로드**: `config_loader.load_operator_profile(path)`가 파일 전체를 문자열로 읽어 `(text, is_default)`를 반환.
2. **주입**: `analyzer.analyze(...)`가 이 문자열을 `_build_prompt`의 `<운영자 프로필>` 블록에 그대로 넣는다. Claude는 이 프로필을 기준으로 importance/relevance/액션을 판단한다.
3. **부재 정책 (명시적 규칙)**:
   - 파일이 없거나 비어 있으면 → **WARNING 로그** 출력 + 모듈 내장 `DEFAULT_PROFILE` 상수를 사용해 파이프라인을 **중단 없이 계속**한다.
   - 이때 리포트 상단과 Discord 메시지에 `"⚠ 운영자 프로필 없음 — 기본 프로필로 분석"`을 명시한다(운영자가 품질 저하를 인지하도록).
   - 무인 실행 안정성을 위해 "기본 프로필 fallback"을 채택한다(오류 중단 대신). 단 fallback 사용 사실은 항상 가시화한다.

**`config/operator_profile.md` 기본 내용(템플릿)** — FEAT-01에서 생성:
```markdown
# 운영자 프로필 — IT상상공방

## 정체성
- IT상상공방: 고객 아이디어를 빠른 MVP(웹/앱/자동화)로 만들어 검증하는 AI 자동 생산 공방.
- 운영자: 1인. AI 도구로 생산 파이프라인을 자동화하는 데 관심.

## 강한 관심 분야 (relevance 높게)
- Claude / Claude Code / Anthropic, MCP, AI Agent
- OpenAI / GPT / Codex
- 바이브코딩, AI 기반 개발 자동화, GitHub Actions 자동화, n8n
- 상상공방 생산 파이프라인에 바로 적용 가능한 도구·사례

## 보통 관심 (importance 위주로 판단)
- Google Gemini, Cursor, 일반 LLM 연구 동향

## 약한 관심 (낮게)
- 단순 자금조달/주가, 가십성 뉴스

## 평가 가이드
- importance: AI 업계 전반 영향도(0~5)
- relevance: 위 관심 분야·상상공방 적용 가능성(0~5)
- 액션 아이템: "상상공방에서 실험해볼 만한가?" 관점으로 제안
```

---

## 7. 실행 플로우

`pipeline.run(args)` 표준 순서 (정상/catch-up 공통):

```
0. --check-sources 분기: 지정 시 collector.check_sources()만 실행·출력하고 종료(섹션 9-1)
1. 설정 로드: load_config()(+경로 env override), load_secrets(), load_operator_profile(), load_sources()
2. 수집 범위 결정 (catch-up):
     start = args.from  > meta.last_success_at  > (now - default_lookback_hours)
     end   = args.to    > now
     catchup_flag = (meta.last_success_at가 default_lookback보다 오래됨) or (--from/--to 지정)
3. 수집: collector.collect(sources, start, end, raw_dir, timeout, test_fixtures=..., save_raw=NOT dry_run)
     - enabled=true 소스만, 소스별 RSS fetch (또는 --test면 fixtures 로드)
     - published_at이 [start,end]에 드는 항목만 채택
     - raw JSON 저장은 save_raw=True일 때만 (──dry-run이면 save_raw=False라 raw도 안 씀──)
     - 반환: (articles, source_errors)
   ── [--dry-run이면 여기서 계획+미리보기만 출력 후 종료. DB·raw JSON·report·Discord 변경 0 (무상태)] ──
4. 저장: storage.insert_article() 루프 → 신규만 DB 삽입 (중복 skip), new_count 집계
5. 분석: analyzer.analyze(new_articles, profile, api_key, model, ..., force_fallback=args.force_fallback)
     - 성공: mode="claude", 기사별 summary/tags/importance/relevance + briefing
     - 실패/강제/키없음/모델명오류: mode="fallback" (섹션 8)
     - storage.update_analysis()로 DB 갱신
6. 리포트: reporter.build_report(...) → reporter.save_report() → reports/<date>/report.md
     - mode, catchup_flag, 수집범위, failed_sources, new_count 반영
7. 전송: args.no_discord가 아니면 notifier.send_discord(webhook, message)
     - webhook 없음/실패 → 경고 로그만, 파이프라인 성공 유지
8. 성공 마감: meta.last_success_at=now, last_report_range_*=(start,end) 갱신
9. 종료코드 0
```

### CLI 옵션 매핑

| 옵션 | 효과 |
|------|------|
| `--test` | 3단계 수집을 `tests/fixtures/sample_articles.json` 로드로 대체 (RSS 미사용) |
| `--dry-run` | 수집 후 계획·미리보기만 출력. **DB·raw JSON·report·Discord 어떤 것도 변경하지 않음(무상태)** |
| `--no-discord` | 7단계 전송 skip (나머지 정상) |
| `--force-fallback` | 5단계 Claude 호출을 강제 실패 처리 → fallback 경로 |
| `--from <ISO>` / `--to <ISO>` | 2단계 수집 범위 수동 지정 (catch-up 재현) |
| `--check-sources` | 파이프라인을 타지 않고 소스 접근성 진단만 실행·출력 후 종료 (섹션 9-1) |

---

## 8. 실패 복구 플로우

| 실패 유형 | 처리 | 데이터 영향 |
|-----------|------|-------------|
| 단일 소스 수집 오류(타임아웃/파싱오류/HTTP오류) | 해당 소스 skip, `source_errors`에 기록, 나머지 진행. 리포트·Discord에 실패 소스 목록 명시 | 다른 소스 정상 저장 |
| Claude 연동 실패(API 오류/타임아웃/키 없음/**모델명 오류·미존재 모델**/JSON 파싱 실패) or `--force-fallback` | `_fallback_analyze()`로 강등: summary=raw_excerpt 앞 N문장, tags=제목 키워드 추출, importance/relevance=0, briefing=규칙기반 요약. 리포트 상단 `"AI 분석 실패 / 기본 리포트 생성"` 명시 | **raw JSON·DB 저장은 이미 완료(분석 단계 이전)** → 유실 0 |
| Discord 전송 실패(webhook 없음/HTTP 오류) | 경고 로그만. 파이프라인 성공 판정은 DB/파일 기준 | 리포트 로컬 저장 완료 유지 |
| DB 쓰기 오류 | 오류 로그 + 실행 중단(종료코드 1). `last_success_at` **미갱신** → 다음 실행이 catch-up | 부분 저장분은 유지, 미완으로 간주 |
| 전체 실행 실패(슬립/크래시) | `last_success_at` 미갱신 상태 → 다음 실행이 그 시각부터 catch-up 수집. 리포트에 보완 수집 + 범위 명시 | — |

**불변식**: raw JSON 저장과 DB 삽입은 Claude 분석 *이전*에 끝난다(단 `--dry-run`은 둘 다 생략). 따라서 Claude/Discord가 죽어도 데이터는 남는다.

**fallback 요약 상세** (`_fallback_analyze`):
- summary: `raw_excerpt`에서 첫 2문장(없으면 제목).
- tags: 제목·excerpt에서 사전 키워드(claude, openai, gpt, mcp, agent, gemini, cursor, automation…) 매칭.
- briefing.summary_text: "기본 리포트" 헤더 + 신규 건수 + 중요 키워드 매칭 상위 제목 나열.

---

## 9. 테스트/검수 플로우

`tests/fixtures/sample_articles.json` — 최소 6건, **중복 URL 1쌍 포함**, published_at 다양(catch-up 범위 검증용). 실제 수집 결과와 동일한 기사 dict 구조.

### 9-1. 소스 접근성 진단 (`--check-sources`)

외부 RSS 검증을 **파이프라인 본체에서 분리한 별도 명령**으로 둔다. 자동 QA는 외부 네트워크에 의존하지 않고 `--test` fixtures로 통과하며, 실제 URL 접근성은 운영자가 이 명령으로 별도 확인한다.

- 명령: `python main.py --check-sources`
- 동작: `sources.yaml`(없으면 `sources.example.yaml`)의 각 소스를 enabled 여부와 무관하게 점검(enabled=false는 표시만), 결과를 표로 콘솔 출력. 종료코드는 모두 성공이면 0, 하나라도 실패면 1.
- 진단 결과 항목(소스당 1행):

| 필드 | 의미 |
|------|------|
| `source_name` | 소스 이름 |
| `url` | 점검 대상 URL |
| `enabled` | sources.yaml에서 켜짐 여부 |
| `http_status` | HTTP 응답 코드(예 200/404), 연결 실패 시 None |
| `ok` | 응답 성공 여부(2xx + 파싱 가능) |
| `timeout` | 타임아웃 발생 여부(bool) |
| `parseable` | feedparser로 RSS/Atom 파싱 가능 여부(엔트리 ≥1) |
| `entry_count` | 파싱된 엔트리 수 |
| `error` | 에러 메시지(없으면 빈 문자열) |
| `checked_at` | 점검 시각 ISO8601 |

출력 예:
```
SOURCE                 ENABLED  HTTP  OK  TIMEOUT PARSE ENTRIES  ERROR
Hugging Face Blog      true     200   ✓   no      yes   30
GitHub Blog            true     200   ✓   no      yes   20
Google Research        true     404   ✗   no      no    0       HTTP 404
...
요약: 성공 6 / 실패 1  → 실패 소스: [Google Research]
```

> 진단은 **읽기 전용**이다(DB/리포트/Discord에 아무것도 쓰지 않음). 점검 결과로 운영자가 `sources.yaml`을 직접 수정한다. 진단 자체는 파이프라인을 중단시키지 않으며, 진단을 돌리지 않아도 정상 실행에서는 부분 실패 정책으로 죽은 소스를 건너뛴다.

### 9-2. 테스트 데이터와 운영 데이터 분리 ★

**pytest는 운영용 `data/morning_brief.db`·`data/raw/`·`reports/`를 절대 건드리지 않는다.** 격리 메커니즘:

- `test_pipeline.py`는 각 테스트 실행 시 환경변수를 세팅해 운영 경로를 덮어쓴다:
  - `MORNINGBRIEF_DB=data/test_morning_brief.db`
  - `MORNINGBRIEF_RAW_DIR=data/raw/test`
  - `MORNINGBRIEF_REPORTS_DIR=reports/test`
- `config_loader.load_config()`가 이 env들을 읽어 `paths.*`를 override한다(섹션 2 참조, FEAT-01 구현).
- 테스트는 `subprocess`로 `main.py`를 호출하며 위 env를 `os.environ` 복사본에 주입한다.
- 리포트 검증 경로도 `reports/test/<date>/report.md`를 본다.
- 테스트 종료 후 `data/test_*`·`reports/test/`는 정리하거나 무시(gitignore). 운영 누적 데이터는 보존된다.

### 9-3. V0.1 성공 기준 = 아래 4개 명령 전부 통과 ★ (reviewer 필수 검증)

자동 QA(6단계) reviewer는 아래 4개 명령을 그대로 실행해 전부 통과해야 V0.1을 완성으로 인정한다.

```bash
# 사전: cd projects/AI-Morning-Brief/05-개발 && pip install -r requirements.txt
#       cp config/sources.example.yaml config/sources.yaml   # 최초 1회
#       (pytest는 자체적으로 테스트 경로 env를 세팅하므로 운영 데이터 무해)

# (1) 정상 파이프라인 (외부 의존 없이 통과, 종료코드 0)
python main.py --test --no-discord ; echo "exit=$?"

# (2) Fallback 경로 (리포트에 'AI 분석 실패 / 기본 리포트 생성' 포함)
python main.py --test --no-discord --force-fallback ; echo "exit=$?"

# (3) Dry-run 무상태 (DB·raw·report·Discord 변경 0, 종료코드 0)
python main.py --test --dry-run ; echo "exit=$?"

# (4) 통합 테스트 (QA-1~6, 운영 데이터와 분리된 경로로 실행)
pytest -q tests/test_pipeline.py
```

> 위 4개가 **V0.1 합격선**이다. 명령 (3) 실행 후 새 리포트 파일·신규 DB 행이 생기지 않아야 한다(무상태 검증).

### 9-4. reviewer 세부 QA 항목 (QA-1~6, 그대로 복붙 가능)

```bash
# QA-1: 샘플 기사가 DB에 저장되는가
python main.py --test --no-discord
sqlite3 data/morning_brief.db "SELECT COUNT(*) FROM articles;"   # > 0

# QA-2: 중복 URL이 두 번 저장되지 않는가 (fixtures에 중복 1쌍 포함)
python main.py --test --no-discord            # 2회 실행해도
python main.py --test --no-discord
sqlite3 data/morning_brief.db "SELECT url,COUNT(*) c FROM articles GROUP BY url HAVING c>1;"   # 결과 없음(0행)

# QA-3: Markdown 리포트가 지정 경로에 생성되는가
python main.py --test --no-discord
ls reports/$(date +%F)/report.md               # 파일 존재

# QA-4: Discord 비활성 상태에서도 오류 없이 완료되는가
python main.py --test --no-discord ; echo "exit=$?"   # exit=0

# QA-5: Fallback 리포트가 문구와 함께 생성되는가
python main.py --test --no-discord --force-fallback
grep -q "AI 분석 실패 / 기본 리포트 생성" reports/$(date +%F)/report.md && echo OK

# QA-6: --from/--to 범위가 리포트에 명시되는가
python main.py --test --no-discord --from "2026-06-01T00:00:00" --to "2026-06-21T23:59:59"
grep -q "2026-06-01" reports/$(date +%F)/report.md && echo OK

# 통합: pytest로 위 6항목 일괄 검증 (테스트 경로 격리)
pytest -q tests/test_pipeline.py

# Dry-run 무상태 확인 (실행 후 reports/reports/test에 새 파일 없음, DB 행 증가 없음)
python main.py --test --dry-run
```

`--test` + `--no-discord` 조합으로 **외부 의존성(RSS/Claude/Discord) 없이 파이프라인 전체가 통과**해야 한다. `--test` 모드에서도 분석은 API 키가 없으면 자동으로 fallback 분석을 쓰도록 해 QA가 키 없이 통과 가능하게 한다.

### 9-5. 실행 환경 (V0.1 = 로컬 Mac, launchd 추천)

`scripts/com.itsangsang.morningbrief.plist` (StartCalendarInterval 04:30):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0"><dict>
  <key>Label</key><string>com.itsangsang.morningbrief</string>
  <key>ProgramArguments</key>
  <array>
    <string>/usr/bin/python3</string>
    <string>/ABS/PATH/projects/AI-Morning-Brief/05-개발/main.py</string>
  </array>
  <key>WorkingDirectory</key><string>/ABS/PATH/projects/AI-Morning-Brief/05-개발</string>
  <key>StartCalendarInterval</key><dict><key>Hour</key><integer>4</integer><key>Minute</key><integer>30</integer></dict>
  <key>StandardOutPath</key><string>/tmp/morningbrief.out.log</string>
  <key>StandardErrorPath</key><string>/tmp/morningbrief.err.log</string>
</dict></plist>
```
설치: `cp scripts/*.plist ~/Library/LaunchAgents/ && launchctl load ~/Library/LaunchAgents/com.itsangsang.morningbrief.plist`
> **launchd 선택 이유**: Mac이 04:30에 슬립이어도 깨어난 직후 놓친 작업을 실행한다(cron은 슬립 시 영구 건너뜀). 이는 catch-up 정책과 맞물려 무인 안정성을 높인다. GitHub Actions/서버 배포는 V0.2 확장으로 분리.

### 9-6. README 필수 안내 (FEAT-08)
README는 다음 순서를 포함한다: ① `pip install -r requirements.txt` ② `cp config/.env.example .env`로 키 입력 ③ `cp config/sources.example.yaml config/sources.yaml` ④ **`python main.py --check-sources`로 RSS 접근성 확인 후 죽은 피드 교체** ⑤ `python main.py --test --no-discord`로 파이프라인 점검 ⑥ launchd 등록. 더불어 **`config.yaml`의 `analysis.model`을 사용 가능한 Anthropic 모델명으로 수정 가능하며, 잘못되면 fallback로 동작함**을 명시한다.

---

## 10. FEAT 목록 (작업 분해 = GitHub 이슈 1:1)

V0.1 기준 8개. 소스 진단 기능은 **별도 FEAT를 추가하지 않고** collector(FEAT-03)에 흡수, CLI 디스패치는 pipeline(FEAT-07)에 둔다(총 8개 유지). 각 행마다 `03-설계/features/FEAT-NN-*.md`가 1:1로 존재한다.

| FEAT/이슈 # | 작업명 | 한 줄 범위 | 의존 |
|-------------|--------|------------|------|
| 01 | 프로젝트 골격 + 설정/프로필 로더 + CLI 스켈레톤 | requirements, 폴더, config.yaml, sources(.example).yaml, operator_profile.md, .env.example, config_loader(load_sources + 경로 env override 포함), main.py argparse(--check-sources 포함) | — |
| 02 | 저장 계층 (SQLite) | storage.py: 스키마 init, INSERT OR IGNORE 중복제거, meta get/set, 범위조회 | 01 |
| 03 | 수집기 (RSS + raw JSON) + 소스 진단 + 샘플 fixtures | collector.py: feedparser 수집·범위필터·raw 저장(**save_raw 옵션**), **check_sources 진단**, --test fixtures, sample_articles.json | 01 |
| 04 | Claude 분석기 + fallback + 프로필 프롬프트 | analyzer.py: 프롬프트 빌드·API 호출·JSON 파싱·fallback(**모델명 오류 포함**)·--force-fallback | 01 |
| 05 | Markdown 리포트 생성기 | reporter.py: 섹션 구성, mode/catchup/실패소스/범위 반영, 날짜별 저장 | 01,02 |
| 06 | Discord Webhook 전송기 | notifier.py: 2000자 브리핑 조립·POST·--no-discord·실패 시 로그만 | 01 |
| 07 | 파이프라인 오케스트레이션 (catch-up + dry-run 무상태 + 실패복구 + check-sources 디스패치) | pipeline.py + main.py 연결: 범위결정, 단계조율, **dry-run 무상태(save_raw=False)**, 정책 적용, meta 갱신, --check-sources 분기 | 01~06 |
| 08 | 스케줄러(launchd) + 통합테스트 + 문서 | plist, tests/test_pipeline.py(QA-1~6, **운영 데이터 분리**), README(--check-sources·모델명 안내 포함), 빌드노트 | 07 |

> builder는 각 FEAT 문서 하나만 읽고 구현한다. 모듈 함수 시그니처는 각 FEAT의 6장에 못박혀 있다.

---

## 11. V0.2 이후 확장 (V0.1 제외 명시)

OpenHands/OpenClaw, Codex QA, GitHub Issue 자동 생성, 양방향 Discord Bot, 웹 대시보드, 벡터DB/RAG, GitHub Trending/Reddit/커뮤니티 수집, JS 렌더링 스크래핑, **Claude Code headless 연동**, **GitHub Actions/서버 배포**는 모두 V0.1 제외, V0.2 이후 확장.

---

## (증분) 주간보고 `--weekly` 설계 — 애드덤

> V0.1(일간 파이프라인) 본문은 그대로 유지한다. 본 섹션은 **신규 기능 증분 `--weekly`(주간 흐름 리포트)** 의 아키텍처 변경점만 기술한다. 방향·설계 결정은 공방장·사용자 검토로 **이미 확정**됐고(실데이터 2회 생성 검증 완료), 여기서는 그 확정 스펙을 코드 형식화하는 근거만 남긴다. 작업 분해는 **FEAT-09 / FEAT-10**(이슈 1:1)으로 펼친다.

### A. 개요와 형태 (변경 없음)
- 형태는 V0.1과 동일한 **자동화 프로세스(Python CLI 파이프라인 + launchd)**. 새 CLI 진입 `python main.py --weekly`를 추가할 뿐 형태/스택 변경은 없다.
- 7일치 일간 데이터(SQLite `articles` + 일간 리포트 `.md`)를 종합 → "이번 주 AI 흐름" 주간보고 생성 → **일간과 동일한 Discord 웹훅**으로 9블록 다이제스트 전송 + 전문 `.md` 저장.

### B. 아키텍처 변경점 (델타만)
```
main.py  --weekly / --week YYYY-W##   ──►  src/weekly.run_weekly(args)   [신규 모듈]
                                              │ 트리거(시간/날짜 + meta 커서, 멱등)
                                              ▼
   storage.get_articles_by_range ─► weekly.aggregate_week  ─┐  (정량 신호 5종)
   reports/<d>.md 7개 ─► weekly.collect_daily_ideas ────────┤
                                                            ▼
                       analyzer.synthesize_weekly (★ _call_claude 재사용, 주간 프롬프트)
                                                            ▼
                       reporter.build_weekly_report ─► reporter.save_bucket_report
                                                            ▼
                       reports/<ISO연도>/weekly/W##.md  +  notifier.build_weekly_message ─► send_discord
                                                            ▼
                       storage.set_meta("last_weekly_iso_week", "YYYY-W##")
```
- **신규 모듈**: `src/weekly.py` (트리거/커서, 집계, 아이디어 통합, 상위기사 선별, 오케스트레이션).
- **추가(수정) 함수**: `analyzer.synthesize_weekly`(+`_build_weekly_prompt`/`_parse_weekly_response`/`_fallback_weekly`, 기존 `_call_claude` 재사용), `reporter.save_bucket_report`+`build_weekly_report`, `notifier.build_weekly_message`, `main.py --weekly/--week` 분기.
- **불변(미변경)**: 일간 `pipeline.run`, 일간 프롬프트/`analyze`, 일간 `build_report`/`save_report`(경로 `<YYYY>/<MM>/<DD>.md` 유지), DB 스키마(articles+meta), 기존 의존성. 모두 **추가만** 한다.

### C. 트리거 — 시간/날짜 + meta 커서 (확정)
- 별도 launchd 잡 `com.itsangsang.morningbrief.weekly`, **일요일 04:40**.
- 대상 주차 = `target_iso_week(now) = (now-2일).isocalendar()`의 (ISO연도, 주차). 일요일 정시 및 +1/+2일 지연 모두 같은 주(월~일)를 가리켜 **자가치유**(Mac sleep 후 wake 보완 실행 대응).
- 멱등: `meta.last_weekly_iso_week`(예 `"2026-W26"`)와 같으면 skip, 다르면 생성 후 갱신. `--week YYYY-W##`는 강제 재생성(커서 일치여도 생성).
- **왜 카운터가 아니라 커서인가**: 카운터(일간 7회==7)는 Mac sleep 미실행·전소스 실패 빈 리포트일·`--from` 재실행에서 일요일 정렬이 깨지고 자가치유 불가 → 기각. 시간/날짜+커서는 늦게 깨어나도 같은 주를 가리키며 커서가 중복을 막는다.
- 커서는 **새 컬럼이 아니라 기존 `meta` 테이블 key-value row**(`last_weekly_iso_week`)로 둔다(스키마 불변).

### D. 정량 신호 레이어 (v2 핵심 — 집계에서만 나오는 신호)
`weekly.aggregate_week`가 코드로 계산해 Claude 합성의 "근거"로 투입(서술이 숫자에 정합):
1. **강도 추이**: 일자별 (건수 / 고중요≥8 / 평균 중요도), 수집 ≤1건 일은 ⚠️ 데이터 품질 경고(이슈 #13의 빈 리포트일과 연결).
2. **태그 빈도 × 지속성**: (총 등장 / 등장 일수). **5일↑ = 지속 줄기**(구조적), 고빈도·저지속 = **단발 버스트**(노이즈).
3. **소스 신호 품질**: 소스별 (건수 / 평균 중요도). 고볼륨·저신호(평균<3.0) = **노이즈 후보**(#12 노이즈 필터·소스 튜닝 입력).
4. **공방 관련 픽**: `relevance ≥ 7`(글로벌 중요도 정렬이 놓치는 공방 맞춤 신호).
5. **일간 아이디어 7일치 통합**: 일간 "상상공방 적용 아이디어" 섹션을 `.md`에서 추출, 반복 등장(우선)→1회성(하위).
- 상위기사: 제목 정규화 중복제거 후 importance desc, relevance desc 상위 ~35건을 합성에 투입.
- **임계값 스케일 주의**: 확정 스펙은 라이브 importance/relevance **0~10** 스케일을 전제(고중요≥8, 공방 픽 relevance≥7, 노이즈 평균<3.0). 설계서 본문 3장의 "0~5" 표기는 V0.1 초기값이며, 임계값은 `weekly.py` 상단 상수(HIGH_IMPORTANCE 등)로 중앙화해 스케일 변경 시 상수만 조정한다.

### E. 전문 저장 경로 (기존 reports 트리 재사용·일반화)
- 일간(기존): `reports/<YYYY>/<MM>/<DD>.md` (변경 없음).
- 주간(신규): `reports/<ISO연도>/weekly/W##.md` (예 `reports/2026/weekly/W26.md`). 연도는 `isocalendar()`의 **ISO 주차연도**(연말연초 어긋남 대응).
- `reporter.save_bucket_report(reports_dir, year, bucket, name, content)` → `reports/<year>/<bucket>/<name>.md` 단일 헬퍼로 일반화. 월간(#12) `monthly/M##.md` 확장 예약.

### F. Discord 다이제스트 (plain content, 2000자 내 완결)
- 기존 `notifier.send_discord`(plain `content` POST) **그대로 재사용**, 임베드/분할 없음(FEAT-06 정책 유지). 표는 전문 `.md`에만, 다이제스트엔 인라인 한 줄 압축.
- 9블록 순서: ①주차+한줄요약 ②강도(총건수·피크·⚠️단절일) ③지속 줄기 Top6 ④노이즈 소스 1줄 ⑤주요 흐름 테마 4줄 ⑥주목 사건 Top3 ⑦공방 즉시 착수 Top3 ⑧다음 주 관전 ⑨전체 리포트 경로. (절삭이 아니라 의도적 요약 — 2000자 내 완결.)

### G. 테스트 / 운영
- 통합테스트 `tests/test_weekly.py`: tmp_path 격리 + env override(`MORNINGBRIEF_DB/RAW_DIR/REPORTS_DIR`) + `--force-fallback` hermetic. 트리거 멱등·ISO 주차 산정(정시/지연)·빈 주·전문 경로·신호 집계 검증.
- 운영: 새 plist `com.itsangsang.morningbrief.weekly`(일요일 04:40, StartCalendarInterval Weekday 0). 운영 배포본은 `~/AI-Morning-Brief-run`(보호폴더 밖)에 rsync 동기화 후 launchctl bootstrap(일간과 동일 패턴). python은 `/Library/Frameworks/Python.framework/Versions/3.14/bin/python3`. 04:30 일간과의 경합은 주간이 읽기 전용·멱등이라 안전(상세 FEAT-10 6-3).

### H. 작업 분해 (FEAT 1:1, 기존 FEAT-01~08에 이어 09·10)
| FEAT/이슈 # | 작업명 | 한 줄 범위 | 의존 |
|-------------|--------|------------|------|
| 09 | 주간보고 집계·생성 엔진 | `weekly.py`(트리거/커서·집계·아이디어 통합·상위선별·run_weekly) + analyzer 주간 합성 + reporter 경로 일반화/전문 빌더 + main `--weekly/--week`. **전문 `.md` 생성까지(Discord 미전송)** | 02·04·05 |
| 10 | 주간 Discord 다이제스트 + 스케줄러 + 통합테스트 | `notifier.build_weekly_message`(9블록) + run_weekly 전송 hook + 일요일 plist + `tests/test_weekly.py` + README 운영 안내 | 09 |

> builder는 각 FEAT 문서 하나(+헌법+이슈)만 읽고 구현한다. FEAT-09는 `--weekly --no-discord`로 전문이 생성되면 완료, FEAT-10은 다이제스트 전송·plist·통합테스트가 통과하면 완료.
