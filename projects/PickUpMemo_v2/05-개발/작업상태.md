# PickUpMemo_v2 개발 작업 상태 (세션 인계용)

> 이 문서만 보고 다음 세션에서 개발을 이어받을 수 있어야 한다.
> 마지막 갱신: 2026-06-17 (이슈 #9 완료 — 개발 루프 종료, QA 진입 게이트 대기)

---

## 0. 현재 상태 (TL;DR)

1. repo: **LeeSiHyeon711/PickUpMemo_v2** (private), 코드: `projects/PickUpMemo_v2/05-개발/PickUpMemo_v2/` (자체 git repo, origin/main)
2. **1차(#1~#9) + 2차 v2.1 개선(#10~#13) 모두 완료.** 이슈 13개 전부 closed (open 0).
3. 현재 = **QA 진입 게이트 대기** → 사람이 GitHub 원격 반영 확인 후 `/승인 PickUpMemo_v2` 입력 시 자동 QA(6단계) 시작
4. 최신 커밋: `250c3c5 FEAT-13...` (local==remote 동기화). APK: `app/build/outputs/apk/debug/app-debug.apk` (clean 빌드 성공, 약 5.8MB)
5. 2차 추가분: #10 팝업검증 테스트화면 / #11 말풍선 아이콘 / #12 배민 로그 저장계층(Room v2, MIGRATION_1_2) / #13 로그 조회·내보내기
4. **빌드 명령** (JDK21 필수):
   ```
   cd projects/PickUpMemo_v2/05-개발/PickUpMemo_v2
   JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew :app:assembleDebug --no-daemon
   ```

## ★ 환경 제약 (중요)
- **백그라운드 하위 에이전트(builder/repo-manager)의 Bash가 런타임에서 차단됨** (bypassPermissions로도 안 풀림). Write/Edit는 정상.
- → builder는 **코드 파일만 작성**, **빌드·git·gh 셸은 공방장(메인 세션)이 전부 대행**한다.
- 설계자 보고서: `~/Desktop/공방_Git권한이슈_보고서.md`

## 빌드 선행조건 (이미 처리됨, 재클론 시 재현 필요)
- `gradle/wrapper/gradle-wrapper.jar` — v1에서 cp(바이너리). properties=gradle-8.7-bin.
- `local.properties` — `sdk.dir=/Users/lsh/Library/Android/sdk` (gitignore됨, repo에 없음)

---

## 1. 완료된 이슈 (closed, push 완료)

| 이슈 | FEAT | 커밋 | 비고 |
|------|------|------|------|
| #1 | 프로젝트 골격 + 빌드/매니페스트 | 482432f | Gradle/매니페스트/스텁, 빌드성공 |
| #2 | 메모 저장 계층 (Room) | ca9d2c5 | Entity/Dao/DB/Repository |
| #3 | 메모 목록 + 삭제 화면 | 2cf72d2 | List/Adapter/MainActivity 진입 |
| #4 | 메모 추가/수정 + 입력 검증 | a4d7a0d | MemoEditActivity 본문, 필수검증 |
| #5 | 업체명 추출·매칭·중복억제 로직 | e327eb7 | StoreExtractor/MemoMatcher/DedupGuard |
| #6 | 오버레이 팝업 컨트롤러 | 1eeb520 | MemoPopupController, 6초 자동닫힘, 태그 GONE |
| #7 | 접근성 서비스 본문 연결 | 9401915 | 이벤트→필터→조립→추출/매칭/억제→팝업 결선 |
| #8 | 권한 안내 (접근성+오버레이) | c96cb77 | PermissionChecker + MainActivity 상태/이동버튼 |
| #9 | 문자열 정리 + APK 빌드 + 빌드노트 | (코드변경 없음) | strings 정합 확인·빌드노트 마감·최종 APK 빌드, 이슈 close |

## 2. 진행 중인 이슈
- 없음

## 3. 남은 이슈
- 없음 (전 이슈 closed, open 0)

## 4. 마지막 성공 빌드
- 명령: `clean :app:assembleDebug` (JDK21) — **BUILD SUCCESSFUL** (이슈 #9, 최종)
- 산출물: `app/build/outputs/apk/debug/app-debug.apk` (약 5.7MB, gitignore·로컬만)

## 5. 현재 브랜치 상태
- 브랜치: `main` (origin/main과 동기화됨, local==remote c96cb77)
- 최신 커밋: `c96cb77 FEAT-08...` (#9는 dev repo 코드 변경 없어 커밋 없음 — strings 정합 확인만, 빌드노트는 repo 밖)
- working tree: clean

## 6. 다음 세션 시작 지점
1. **개발 끝.** 사람이 `/승인 PickUpMemo_v2` 입력 시 자동 QA(reviewer, 6단계) 시작
2. QA 후 사람 수동 테스트 게이트 → 증상 있으면 `/증상`, 통과면 `/납품`
3. **남은 open 이슈 0 되면 멈춤** → QA 진입 게이트: 공방장이 Git 검증 체크리스트 출력 후 사람 `/승인 PickUpMemo_v2` 대기 (자동 QA로 바로 넘어가지 않음)

## 7. 스택 / 규약
- Kotlin 1.9.24 / AGP 8.5.2+Gradle 8.7 / minSdk26 targetSdk34 / Room 2.6.1+KSP / XML뷰+ViewBinding / 오버레이 TYPE_APPLICATION_OVERLAY
- 패키지: `com.itmakesome.pickupmemo2`
- 금지: 서버/DB(원격)/클라우드/AI/OCR/INTERNET권한, v1 알림리스너·로그저장·내보내기·테스트알림 이식, Compose/DI/네트워크 라이브러리
- 커밋 메시지 말미: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
